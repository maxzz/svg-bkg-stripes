{
    "name": "svg-bkg-stripes",
    "description": "SVG generation playground",
    "version": "0.0.1",
    "scripts": {
        "dev": "vite",
        "build": "vite build",
        "predeploy": "rm -rf dist && yarn build",
        "deploy": "gh-pages -d dist"
    },
    "dependencies": {
        "vue": "^3.0.2"
    },
    "devDependencies": {
        "@vue/compiler-sfc": "^3.0.2",
        "sass": "^1.28.0",
        "vite": "^1.0.0-rc.9"
    }
}